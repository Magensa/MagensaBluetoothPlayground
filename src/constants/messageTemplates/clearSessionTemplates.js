import { magTekDevice, dotInterface } from './index';

const clearSession = `clearSession`;
const clearResp = `${clearSession}Resp`;
const clearSessionScra = `Successfully executed 'clearSession' on a SCRA Device`;

export {
    magTekDevice,
    dotInterface,
    clearSession,
    clearResp,
    clearSessionScra
}